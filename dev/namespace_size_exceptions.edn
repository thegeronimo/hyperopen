[{:path "src/hyperopen/margin_rec/state.cljs" :owner "platform" :reason "Batch liquidation-risk top-up selectors (batch-candidates, per-dex available pools, coverage) joined the existing margin-rec state owner so the toolbar panel and apply action share one pure planning surface; split batch selectors into a dedicated namespace during margin-rec cleanup." :max-lines 640 :retire-by "2026-12-31"}
 {:path "src/hyperopen/account/history/position_margin.cljs" :owner "platform" :reason "Architecture remediation wave baseline oversized namespace pending split or tightened facade. prepare-submit now carries local-only :position metadata (coin/dex/side) on the built request so the margin success toast can name which position moved; extract the request builder during the split." :max-lines 526 :retire-by "2026-12-31"}
 {:path "src/hyperopen/asset_selector/actions.cljs" :owner "platform" :reason "Architecture remediation wave baseline oversized namespace pending split or tightened facade." :max-lines 642 :retire-by "2026-12-31"}
 {:path "src/hyperopen/asset_selector/markets.cljs" :owner "platform" :reason "Grouped outcome market metadata decoding, question rows, option-side routing, category summaries, and Hyperliquid parity aliases stayed with the existing asset selector market model owner; split outcome-specific parsers and builders during dedicated asset-selector cleanup. Spot wire asset-id (10000 + spotMeta pair index) encoding added to the spot market builder for spot trading; the spot-only coin resolver (resolve-spot-market-by-coin, so spot balances never adopt a dual-listed token's perp market) extended it again; split during the asset-selector cleanup." :max-lines 1330 :retire-by "2026-12-31"}
 {:path "src/hyperopen/asset_selector/markets_cache.cljs" :owner "platform" :reason "Grouped outcome cache normalization preserves question options, aliases, and subscription coins in the existing asset selector cache owner; extract shared outcome cache codecs during dedicated asset-selector cleanup." :max-lines 546 :retire-by "2026-12-31"}
 {:path "src/hyperopen/core/public_actions.cljs" :owner "platform" :reason "Outcome category and side-market parity actions extended the central public action dispatch facade; split outcome action registration during dedicated public-action boundary cleanup." :max-lines 503 :retire-by "2026-12-31"}
 {:path "src/hyperopen/domain/trading/market.cljs" :owner "platform" :reason "Architecture remediation wave baseline oversized namespace pending split or tightened facade. Spot trading added side-aware sizing (max-order-base-size), spot balance helpers (USDC/base available), and spot-aware order-summary (omits margin/liquidation); extract a spot-sizing helper namespace during the trading-domain cleanup." :max-lines 574 :retire-by "2026-12-31"}
 {:path "src/hyperopen/funding/application/modal_commands.cljs" :owner "platform" :reason "CRAP remediation for hyperopen-7y38 kept funding modal command normalization and dependent-reset ownership in the existing command namespace; a larger command/helper split is deferred to dedicated funding-boundary cleanup. Named DEX (HIP-3) perps transfer context threading on open-funding-transfer-modal extended this owner; selected-subaccount transfer-source identity (fromSubAccount/destination for unified-master + classic-subaccount transfers) extended it again; split stays deferred." :max-lines 560 :retire-by "2026-12-31"}
 {:path "src/hyperopen/schema/funding_modal_contracts.cljs" :owner "platform" :reason "Funding modal VM contract intentionally centralizes nested spec shapes for tyy8; keep it co-located until the next schema cleanup pass can split shared helpers without obscuring the single contract owner." :max-lines 799 :retire-by "2026-12-31"}
 {:path "src/hyperopen/order/actions.cljs" :owner "platform" :reason "Passkey unlock-and-replay cancel continuation and outcome option dropdown UI actions stayed with the existing order action owner; split submit/cancel lifecycle and order-form UI helpers during dedicated order-action cleanup. The isolated-margin auto top-up intent seam (margin-rec-intent-save wired into every submit branch) extended it again." :max-lines 595 :retire-by "2026-12-31"}
 {:path "src/hyperopen/order/effects.cljs" :owner "platform" :reason "Locked-agent, expiry-aware trading guards, and Hyperliquid subaccount owner/vault routing stayed with the existing order effects owner; a broader order-effects split is deferred to dedicated boundary cleanup. Threading the per-position margin success and failure toast payloads (built from the submitted request) into the submit-response/runtime-error handlers extended it again." :max-lines 795 :retire-by "2026-12-31"}
 {:path "src/hyperopen/portfolio/fee_schedule.cljs" :owner "platform" :reason "Expanded portfolio fee schedule market-type modeling, active HIP-3 deployer context derivation, and rate-row scenario composition stayed with the existing pure fee-schedule owner; split market-option context helpers during a dedicated portfolio fee-schedule cleanup." :max-lines 680 :retire-by "2026-12-31"}
 {:path "src/hyperopen/api/instance.cljs" :owner "platform" :reason "Referrals account API wiring extends the existing injectable account operations facade; split account operation registration during dedicated API instance cleanup." :max-lines 502 :retire-by "2026-12-31"}
 {:path "src/hyperopen/schema/contracts/action_args.cljs" :owner "platform" :reason "Optimizer draft actions, trading chart candle backfill, portfolio + vault Monte Carlo control args, and subaccount management action args extended the central action payload contract map; split feature action argument specs during dedicated schema contract cleanup. The named DEX funding transfer open-args spec extended this central contract; the subaccounts Refresh force-load action contract extended it again; referrals route, form, and modal action contracts extended it again; the set-ui-theme appearance action contract extended it again; outcome option dropdown action args extended it again; the reset-degen-life contract extended it again; the optimizer target-sigma action contracts extended it again; the optimizer draft-result refinement action contracts (open/close/set-depth/refine/stop) extended it again; the USDH borrow-repay action contract extended it again; the spectate-mode watchlist import/export action contracts (export/import/apply-imported) extended it again; the optimizer history-assumption action contracts (mode/return/volatility/proxy-instrument/relationship/max-weight-cap/clear) extended it again; the optimizer execution-tab action contracts (open/phase/default-order-type/row-order-type/toggle-row/row-param) extended it again; the optimizer execution recovery + run-control contracts (resume/revert/restage/pause/discard/open-in-ticket/order-filter) extended it again; the optimizer exposure-map constraint contracts (exposure-point/exposure-band/exposure-preset/reset-to-system, plus the wallet+universe constraint-profile save/load/apply) extended it again; the optimizer exposure-map zoom-level contract extended it again; split stays deferred. The engine-backed proxy history-assumption action contracts (proxy-asset toggle, relationship strength, apply, reset) extended it again; the assumption-card collapse + library-hydrate contracts extended it again; the return-views file export/import contracts (export/import/apply-imported/dismiss-note) extended it again.; the scenario-library menu contracts (toggle/close/keydown/new-scenario) extended it again; the history-assumptions file export/import contracts (export/import/apply-imported/dismiss-note) extended it again; the backend-recommended history-assumption apply contracts (single-card + bulk) extended it again; the Equal Risk selected-risk-instrument contract extended it again; the isolated-margin recommendation action contracts (sync/process-intents/toggle-panel/risk-mode/auto-topup) extended it again; the recommendation popover close/keydown contracts and the anchored toggle-panel arg spec (accepting the raw bounds placeholder) extended it again; the batch liquidation-risk top-up contracts (toggle/close/keydown/toggle-selection/apply) extended it again; the optimizer switch-objective-and-run cross-link contract extended it again" :max-lines 735 :retire-by "2026-09-30"}
 {:path "src/hyperopen/runtime/effect_order_contract.cljs" :owner "platform" :reason "Optimizer pipeline actions, trading chart candle backfill, trader-address portfolio benchmarks, and subaccount management actions need explicit heavy-effect ordering in the central runtime contract; the subaccounts Refresh force-load action adds its own heavy-effect ordering policy; referrals route and signed mutation effects extended the same formal runtime policy; the USDH borrow-repay heavy-effect ordering policy extended it again; the isolated-margin recommendation policies (background sync fan-out, one-shot intent top-ups) extended it again; the batch liquidation-risk top-up policy (one updateIsolatedMargin per selected position) extended it again; split feature-specific policies during dedicated formal-boundary cleanup." :max-lines 710 :retire-by "2026-12-31"}
 {:path "src/hyperopen/runtime/effect_adapters.cljs" :owner "platform" :reason "Trader-address portfolio benchmark and subaccount management effect wiring stayed with the central effect-adapter facade to match existing API, vault, and account adapter ownership; the subaccounts Refresh force-load effect adapter, master/owner-mode abstraction fetch, owner snapshot fetch, and referral effect facade exports extended it again; the USDH borrow-repay effect facade extended it again; split portfolio/account-specific API effects during dedicated runtime-adapter cleanup.; the optimizer reset-draft (New scenario) effect facade extended it again" :max-lines 630 :retire-by "2026-12-31"}
 {:path "src/hyperopen/subaccounts/effects.cljs" :owner "platform" :reason "Subaccount load, refresh, mutation, owner-mode routing, and owner-snapshot balance ownership stay in the existing subaccount side-effect boundary for the selected-subaccount balance/transfer regression; split route-load snapshot helpers from mutation helpers during dedicated subaccounts-effect cleanup." :max-lines 567 :retire-by "2026-12-31"}
 {:path "src/hyperopen/staking/actions.cljs" :owner "platform" :reason "CRAP remediation for hyperopen-7y38 kept staking balance normalization helpers with the existing action owner pending a later staking-boundary split." :max-lines 700 :retire-by "2026-12-31"}
 {:path "src/hyperopen/state/trading.cljs" :owner "platform" :reason "Grouped outcome option routing extended the existing trading selector and order context owner; split instrument-specific context derivation during dedicated trading-state cleanup. Spot trading hardened asset-id resolution to never leak the raw (colliding) spot index; split during the trading-state cleanup." :max-lines 722 :retire-by "2026-12-31"}
 {:path "src/hyperopen/startup/runtime.cljs" :owner "platform" :reason "Dev hot-reload lifecycle ownership stayed inside the existing startup runtime boundary for xw1; the disconnected spectate reset and trading settings global shortcut extended the same lifecycle owner, and split or facade tightening is deferred to dedicated startup-boundary cleanup." :max-lines 572 :retire-by "2026-12-31"}
 {:path "src/hyperopen/telemetry/console_preload.cljs" :owner "platform" :reason "Architecture remediation wave baseline oversized namespace pending split or tightened facade. Dispatch-error QA observability (recent swallowed nexus dispatch errors surfaced in the qa-snapshot plus HYPEROPEN_DEBUG.dispatchErrors/clearDispatchErrors accessors) extended this owner; extract the debug-api/qa-snapshot assembly during dedicated console-preload cleanup." :max-lines 550 :retire-by "2026-12-31"}
 {:path "src/hyperopen/trading/order_form_transitions.cljs" :owner "platform" :reason "Architecture remediation wave baseline plus outcome option dropdown state transitions stayed with the order-form transition owner; split UI dropdown transitions during dedicated order-form cleanup." :max-lines 719 :retire-by "2026-12-31"}
 {:path "src/hyperopen/views/chart/d3/runtime.cljs" :owner "platform" :reason "Architecture remediation wave baseline oversized namespace pending split or tightened facade." :max-lines 711 :retire-by "2026-12-31"}
 {:path "src/hyperopen/views/spectate_mode_modal.cljs" :owner "platform" :reason "Architecture remediation wave baseline oversized namespace pending split or tightened facade." :max-lines 612 :retire-by "2026-12-31"}
 {:path "src/hyperopen/views/active_asset/row.cljs" :owner "platform" :reason "Outcome sub-market selector placement moved into the active market strip to match Hyperliquid parity while reusing order-form selector controls; hover-scope isolation for the sub-market selector stayed in this row owner. Split outcome row controls from the active-asset row during dedicated trade-header cleanup." :max-lines 511 :retire-by "2026-12-31"}
 {:path "src/hyperopen/portfolio/optimizer/application/execution.cljs" :owner "platform" :reason "Execution domain core co-locates the plan/attempt builders (ready/blocked/skipped classification, per-row wire-size flooring + order-request build), the order-type selection application, the settled-response status model (response-ok?, settled-row-status classifying HL :filled => filled vs :resting => open-on-book, realized-fill scaling realized $ slippage to the actually-filled totalSz), the passive (post-only/ALO) price built from the live book's own-side touch so it rests rather than crossing the BBO plus the reprice-on-reject parser (post-only-cross-bbo / reprice-post-only-action) that re-rests a crossed post-only order at the live touch carried in the rejection, the resting-aware final-ledger-status, and the recovery plan builders (resume skips filled/resting live rows, revert reverses only filled legs, re-stage drops live rows + scales the rest); the live-fill reconciliation extraction (public coin-for-row, resting-oid reading the oid from the settled resting response, and realized-from-avg factored out of realized-fill so the view-model overlay that promotes a resting order to filled from [:orders :fills] produces an identical :realized shape) extended it again. The marketable IOC price (opposite-side touch padded by the market slippage tolerance, so thin named-dex books actually cross) extended it again. Split the status/response model from the plan/recovery builders during dedicated optimizer domain cleanup. The allocator-excluded hold-row mapping (:excluded preview rows land in the skipped section, never as orders) extended it again." :max-lines 640 :retire-by "2026-09-30"}
 {:path "src/hyperopen/views/portfolio/optimize/execution_tab.cljs" :owner "platform" :reason "Execution surface co-locates the header + phase control bands (staged/armed/running/done/halted/resting) with the armed-band commit figures + live-commit button, the running Pause/abort acknowledgement, the 8-KPI strip (orders / notional / buys / sells / margin / est-or-realized price cost / fees / all-in cost; the buys/sells directional-flow pair was ported in when the standalone Rebalance preview tab was retired and its CTAs began staging straight into Execution), the execution-health diagnostics rail, the live type-aware price-cost+fee recompute (resting => no spread/impact + maker fee, crossing => spread+impact + taker fee), and the latest-attempt panel. The dense per-order table + inline per-order type editor + per-row cost breakdown were extracted to hyperopen.views.portfolio.optimize.execution-order-table, and the shared pure helpers (formatting, order-type, chip/eyebrow) to hyperopen.views.portfolio.optimize.execution-shared, during the optimizer usability-hardening pass (2026-06-29). Remaining split candidate: the KPI strip + health rail into their own namespace during dedicated optimizer view cleanup. The armed-band isolated-margin auto top-up toggle extended it again. The partial phase (rejection alongside still-live resting orders) added a band variant + rail note (2026-07-13). The book-data health diag (2026-07-27) — how many rows are priced from a live book, its age, and an explicit warning when every cost is a flat assumption — extended it again." :max-lines 840 :retire-by "2026-09-30"}
 {:path "src/hyperopen/views/trade/order_form_controls.cljs" :owner "platform" :reason "Outcome option dropdown, searchable live option rows, opaque market-strip menu styling, Yes/No side controls, and degen leverage/size risk wiring stayed with the existing order-form control cluster pending a later helper split or tightened facade." :max-lines 803 :retire-by "2026-12-31"}
 {:path "src/hyperopen/views/trading_chart/utils/chart_interop/chart_context_menu_overlay.cljs" :owner "platform" :reason "Trading chart right-click context-menu parity for hyperopen-mjsz introduced a chart-local overlay owner parallel to navigation overlays; subsequent accessibility and copy-precision hardening kept focus, input gating, and precise copy formatting co-located until dedicated chart-overlay cleanup extracts shared DOM/menu helpers." :max-lines 844 :retire-by "2026-12-31"}
 {:path "src/hyperopen/views/trading_chart/utils/chart_interop/chart_navigation_overlay.cljs" :owner "platform" :reason "CRAP remediation for hyperopen-7y38 kept chart navigation hover geometry helpers with the overlay interop owner; broader chart interop splitting remains deferred." :max-lines 690 :retire-by "2026-12-31"}
 {:path "src/hyperopen/views/trading_chart/utils/chart_interop/visible_range_persistence.cljs" :owner "platform" :reason "The symbol-load visible-range fix added seed-aware persist gating (allow-persist-fn) and live-edge recentering of persisted windows to the existing visible-range persistence owner so restore, validation, and persistence stay one surface; split storage codecs from time-scale sampling during dedicated chart-interop cleanup." :max-lines 580 :retire-by "2026-12-31"}
 {:path "src/hyperopen/vaults/effects.cljs" :owner "platform" :reason "hyperopen-k63w updated vault-transfer trading-lock gating inside the existing vault effects owner; broader vault-effects splitting is deferred to dedicated cleanup." :max-lines 666 :retire-by "2026-12-31"}
 {:path "src/hyperopen/vaults/detail/activity.cljs" :owner "platform" :reason "The Monte Carlo activity tab added a tab entry and matching no-table tab-config to the shared vault activity model (already at the size limit); split the tab-config catalog from the sort/filter helpers during dedicated vault activity cleanup." :max-lines 505 :retire-by "2026-12-31"}
 {:path "src/hyperopen/wallet/agent_session.cljs" :owner "platform" :reason "hyperopen-kmsr split passkey metadata sanitation and persistence helpers to remove CRAP hotspots while keeping browser-storage ownership centralized; a later pass can peel metadata codecs into a narrower namespace." :max-lines 570 :retire-by "2026-12-31"}
 {:path "test/hyperopen/api_wallets/effects_test.cljs" :owner "platform" :reason "Architecture remediation wave baseline oversized namespace pending split or tightened facade." :max-lines 589 :retire-by "2026-12-31"}
 {:path "test/hyperopen/asset_selector/actions_test.cljs" :owner "platform" :reason "Outcome side subscription ordering coverage stayed with the existing selector action suite to preserve exact effect-order assertions; split asset-switch effect fixtures during dedicated selector test cleanup." :max-lines 525 :retire-by "2026-12-31"}
 {:path "test/hyperopen/schema/contracts/action_args_test.cljs" :owner "platform" :reason "Central action-args validation suite asserts payload contracts across many feature actions in one harness; the optimizer execution-tab action contracts (open/phase/default-order-type/row-order-type/toggle-row/row-param) extended it. Split feature action-arg validation suites during dedicated schema contract test cleanup." :max-lines 525 :retire-by "2026-12-31"}
 {:path "test/hyperopen/portfolio/optimizer/application/execution_test.cljs" :owner "platform" :reason "Execution domain test suite covers the plan/attempt builders (ready/blocked/skipped classification, wire-size flooring, order-type routing, spot/perp asset-id), the passive book-touch + crossed-book fallback pricing, the post-only reprice-on-reject parser, the settled-response status model (settled-row-status, resting vs filled, partial-fill realized slippage), the resting-aware final-ledger-status, and the recovery plan builders (resume/revert/re-stage live-row handling) in one harness. Split the pricing/status tests from the plan/recovery tests during dedicated optimizer test cleanup. The allocator-excluded skipped-row regression extended it again." :max-lines 610 :retire-by "2026-12-31"}
 {:path "test/hyperopen/portfolio/optimizer/application/view_model_test.cljs" :owner "platform" :reason "Optimizer view-model projection suite covers scenario/workspace/universe/tracking/execution-modal/execution-tab/inputs models in one harness; execution-tab perp+spot display-label resolution coverage (HIP-3 trailing-coin + spotMeta @N dust) plus live run-attempt display selection and the resting (open-orders-on-book) terminal-phase projection extended it. Split per-model projection suites during dedicated optimizer view-model test cleanup." :max-lines 575 :retire-by "2026-12-31"}
 {:path "src/hyperopen/portfolio/optimizer/domain/rebalance.cljs" :owner "platform" :reason "Rebalance-preview domain co-locates row classification, cost estimation, and the margin/leverage projection; carrying the tolerance band onto within-tolerance rows (so the execution ledger can say WHY a leg is skipped) nudged it past the 500-line default. Split cost-estimation from row classification during dedicated optimizer domain cleanup. The TWAP sliced-execution cost model (venue 30s suborder cadence + permanent-impact residue) and the bounded depth-extrapolation floor (book-edge-anchored capped remainder with overrun/coverage disclosure metadata) extended the cost-estimation block again. The allocator-excluded hold rows (nil target => :excluded held row, never a sell-to-zero) extended the row-classification block again." :max-lines 675 :retire-by "2026-12-31"}
 {:path "test/hyperopen/portfolio/optimizer/domain/rebalance_test.cljs" :owner "platform" :reason "Rebalance-preview domain suite covers row classification, signed-delta-across-zero gross math, live-orderbook + fallback cost estimation, and the commit-moment margin map; the account-leverage reframe added a full exact-map assertion plus an account-effective-leverage / leverage-projection regression. Split cost-estimation from margin/leverage tests during dedicated optimizer domain test cleanup. The bounded depth-extrapolation floor (exact-value + cap + sell-bound regressions) and the TWAP cost-model suite (cadence parity, slicing, duration monotonicity, flat passthrough, clip-overrun flag) extended it again. The allocator-excluded hold-row regression (nil target never stages a sell-to-zero) extended it again. The maker-fee honesty regression (a build site that omits the maker-fee assumption leaves the keys off the cost map instead of stamping 0) extended it again." :max-lines 725 :retire-by "2026-09-30"}
 {:path "test/hyperopen/core_bootstrap/order_entry_actions_test.cljs" :owner "platform" :reason "Passkey unlock-and-replay cancel coverage for hyperopen-z9uh extended the existing order-entry action contract suite; split cancel-specific action coverage during dedicated order test-boundary cleanup." :max-lines 900 :retire-by "2026-12-31"}
 {:path "test/hyperopen/core_bootstrap/order_effects/cancel_test.cljs" :owner "platform" :reason "Open Orders cancel stale-writer guard and passkey unlock-and-replay coverage stayed with the existing order-cancel effects owner; split the cancel effect suite during dedicated order test-boundary cleanup." :max-lines 600 :retire-by "2026-12-31"}
 {:path "test/hyperopen/funding/actions_test.cljs" :owner "platform" :reason "CRAP remediation for hyperopen-7y38 extended funding modal regression coverage in the existing action test owner; root surface lazy-load coverage for hyperopen-5o4d added effect-order expectations, and splitting this suite is deferred to dedicated funding test-boundary cleanup." :max-lines 1320 :retire-by "2026-12-31"}
 {:path "test/hyperopen/portfolio/optimizer/application/engine_test.cljs" :owner "platform" :reason "Optimizer engine coverage still exercises objective, frontier, diagnostics, and constraint behaviors in one suite; the worker-preview default-fee-bps coverage (nonzero fees from the derived schedule) extended it again; split engine fixtures and focused solver suites during dedicated optimizer engine test cleanup. The engine-backed proxy history-assumption integration test (proxy-synthesized covariance row, cap enforcement, low tiny-sample confidence, assumption expected-return) extended it again." :max-lines 640 :retire-by "2026-12-31"}
 {:path "test/hyperopen/portfolio/optimizer/application/history_loader_api_v2_test.cljs" :owner "platform" :reason "Optimizer history loader API v2 coverage grew past the limit in the proxy-id migration; split request-shape fixtures during dedicated optimizer history test cleanup." :max-lines 600 :retire-by "2026-12-31"}
 {:path "test/hyperopen/portfolio/optimizer/universe_actions_test.cljs" :owner "platform" :reason "Optimizer universe action coverage now includes side-selection mutation, rerun contracts, and exclusion-with-rerun coverage beside add/remove/from-holdings coverage; instrument removal now also clears stale per-asset history assumptions; the holdings-by-default work added universe-source recording coverage (holdings omission accounting, custom flip on hand-adds) and the clear-universe escape-hatch test; split side-selection fixtures during dedicated optimizer action test cleanup." :max-lines 640 :retire-by "2026-12-31"}
 {:path "test/hyperopen/platform/indexed_db_test.cljs" :owner "platform" :reason "IndexedDB helper coverage now includes legacy optimizer-store migration and stale cached connection upgrade behavior; split migration/cache tests during dedicated browser-storage test cleanup." :max-lines 511 :retire-by "2026-12-31"}
 {:path "test/hyperopen/runtime/effect_adapters/portfolio_optimizer_pipeline_test.cljs" :owner "platform" :reason "Pipeline adapter coverage added the prefetch idle-wait default regression (production env passes no idle poll/timeout overrides) beside the existing pipeline harness; split prefetch fixtures during dedicated optimizer pipeline test cleanup." :max-lines 542 :retire-by "2026-12-31"}
 {:path "test/hyperopen/runtime/effect_adapters/portfolio_optimizer_scenarios_test.cljs" :owner "platform" :reason "Scenario persistence adapter coverage now includes save-modal IndexedDB write, write-failure, and route-replacement behavior in the existing adapter harness; split scenario persistence success and failure suites during dedicated optimizer storage test cleanup.; the save-anytime coverage (workspace id reuse, no-navigation, reset-draft fresh start) extended it again" :max-lines 623 :retire-by "2026-12-31"}
 {:path "test/hyperopen/subaccounts/effects_test.cljs" :owner "platform" :reason "Subaccount effects coverage now includes the Refresh force-load path (tokenized dedupe bypass + refreshing-flag clearing on success and error) and the master/owner-mode transfer-routing regressions (mixed-mode deposit/withdraw + owner-mode load) alongside the existing create/rename/transfer suites; split management vs load/refresh suites during dedicated subaccounts test cleanup." :max-lines 687 :retire-by "2026-12-31"}
 {:path "test/hyperopen/startup/runtime_test.cljs" :owner "platform" :reason "Architecture remediation wave baseline oversized namespace pending split or tightened facade; disconnected spectate reset, trading settings shortcut coverage, and cancel-guard reset coverage stayed with the existing runtime lifecycle owner." :max-lines 1272 :retire-by "2026-12-31"}
 {:path "test/hyperopen/wallet/agent_runtime_test.cljs" :owner "platform" :reason "hyperopen-k63w, hyperopen-zzz1, and hyperopen-kmsr kept the passkey lockbox, migration, unlock guard, and cleanup regression coverage co-located with the wallet runtime owner; split helper fixtures or narrower focused suites later." :max-lines 900 :retire-by "2026-12-31"}
 {:path "test/hyperopen/wallet/agent_runtime_edge_test.cljs" :owner "platform" :reason "Wallet agent runtime edge-case coverage is currently co-located with the focused runtime boundary suite; split edge fixtures and narrower unlock or cleanup cases during dedicated wallet test-boundary cleanup." :max-lines 532 :retire-by "2026-12-31"}
 {:path "test/hyperopen/wallet/core_test.cljs" :owner "platform" :reason "xw1 added provider-listener reload coverage to the existing wallet lifecycle test owner; subsequent wallet lifecycle assertions stayed co-located pending a dedicated wallet core test split." :max-lines 597 :retire-by "2026-12-31"}
 {:path "test/hyperopen/trading/order_form_transitions_test.cljs" :owner "platform" :reason "Architecture remediation wave baseline oversized namespace pending split or tightened facade." :max-lines 621 :retire-by "2026-12-31"}
 {:path "test/hyperopen/vaults/effects_test.cljs" :owner "platform" :reason "Architecture remediation wave baseline oversized namespace pending split or tightened facade." :max-lines 1105 :retire-by "2026-12-31"}
 {:path "test/hyperopen/views/account_equity_view_test.cljs" :owner "platform" :reason "Architecture remediation wave baseline oversized namespace pending split or tightened facade; disconnected no-account placeholder coverage stayed with the existing account equity view owner." :max-lines 620 :retire-by "2026-12-31"}
 {:path "test/hyperopen/views/footer_view_test.cljs" :owner "platform" :reason "Architecture remediation wave baseline oversized namespace pending split or tightened facade." :max-lines 815 :retire-by "2026-12-31"}
 {:path "test/hyperopen/views/funding_modal_test.cljs" :owner "platform" :reason "Architecture remediation wave baseline oversized namespace pending split or tightened facade." :max-lines 558 :retire-by "2026-12-31"}
 {:path "test/hyperopen/views/app_view_test.cljs" :owner "platform" :reason "Vault route-loading regression coverage stayed with the existing app-shell owner; referrals route dispatch coverage extended the same app-shell route owner; a broader app-view test split is deferred to dedicated test-boundary cleanup." :max-lines 521 :retire-by "2026-12-31"}
 {:path "test/hyperopen/views/header_view_test.cljs" :owner "platform" :reason "Desktop header navigation regression coverage stayed with the existing header view owner; subaccount More-menu coverage remains co-located, and referrals nav coverage extended the same suite until a broader header view test split lands." :max-lines 630 :retire-by "2026-12-31"}
 {:path "test/hyperopen/views/account_info/vm_test.cljs" :owner "platform" :reason "Outcome tab projection and active-context enrichment coverage extended the existing account-info VM contract suite; split account-info VM fixtures and selected-tab cases during dedicated account-info test cleanup." :max-lines 580 :retire-by "2026-12-31"}
 {:path "test/hyperopen/views/l2_orderbook_view_test.cljs" :owner "platform" :reason "Architecture remediation wave baseline oversized namespace pending split or tightened facade." :max-lines 888 :retire-by "2026-12-31"}
 {:path "test/hyperopen/views/portfolio/vm/benchmarks_test.cljs" :owner "platform" :reason "Trader-address benchmark selector and chart/performance coverage stayed with the existing portfolio benchmark VM regression suite; split selector-source fixtures during dedicated portfolio VM test cleanup." :max-lines 870 :retire-by "2026-12-31"}
 {:path "test/hyperopen/views/portfolio/optimize/scenario_detail_view_test.cljs" :owner "platform" :reason "Optimizer scenario detail view coverage now includes save-modal, route-retention, stale-output retention, automatic recompute, tab rendering contracts, allocation side-selection controls, and rebalance-discoverability CTA coverage (header action + recommendation CTA + inactive-tab contrast + result-confidence-rail next-step rebalance) — re-pointed to stage straight into Execution after the standalone Rebalance preview tab was retired — and the plain-language verdict + single-primary CTA hierarchy coverage plus the 2026-07-13 CTA-prominence pins (animated-border modifier gating and tooltip-demoted guidance) in the existing scenario-detail harness; split route-state and tab-specific suites during dedicated optimizer view test cleanup." :max-lines 1060 :retire-by "2026-12-31"}
 {:path "test/hyperopen/views/trading_chart/core_test.cljs" :owner "platform" :reason "Trading chart right-click context-menu parity for hyperopen-mjsz kept chart lifecycle regression coverage with the existing core test owner, and the follow-up copy-precision wiring assertion stayed with the same mount/update harness; extracting narrower fixture-backed suites is deferred to dedicated trading-chart test cleanup." :max-lines 1333 :retire-by "2026-12-31"}
 {:path "test/hyperopen/views/trading_chart/utils/chart_interop/position_overlays_test.cljs" :owner "platform" :reason "Architecture remediation wave baseline oversized namespace pending split or tightened facade." :max-lines 679 :retire-by "2026-12-31"}
 {:path "test/hyperopen/views/vaults/detail_vm_test.cljs" :owner "platform" :reason "Architecture remediation wave baseline oversized namespace pending split or tightened facade." :max-lines 569 :retire-by "2026-12-31"}
 {:path "test/hyperopen/websocket/client_test.cljs" :owner "platform" :reason "Architecture remediation wave baseline oversized namespace pending split or tightened facade." :max-lines 615 :retire-by "2026-12-31"}
 {:path "test/hyperopen/websocket/endpoints_coverage_test.cljs" :owner "platform" :reason "Architecture remediation wave baseline oversized namespace pending split or tightened facade." :max-lines 829 :retire-by "2026-12-31"}
 {:path "test/hyperopen/websocket/user_test.cljs" :owner "platform" :reason "Architecture remediation wave baseline oversized namespace pending split or tightened facade; +5 lines from base-dex clearinghouseState stream fixtures (2026-07-14)." :max-lines 781 :retire-by "2026-12-31"}
 {:path "src/hyperopen/portfolio/actions.cljs" :owner "platform" :reason "Portfolio fee schedule scenario selectors, anchor-normalization fallback, volume-history popover integration, trader-address benchmark action effects, and Monte Carlo account-tab options share the existing portfolio UI action owner; split selector-specific command helpers during a dedicated portfolio action-boundary cleanup." :max-lines 672 :retire-by "2026-12-31"}
 {:path "src/hyperopen/portfolio/optimizer/actions/draft.cljs" :owner "portfolio" :reason "The per-asset history-assumption setters were added beside the existing objective-menu, constraint, and asset-override draft mutations; the return-views consolidation then made the inline view-row actions materialize authored views immediately (upsert/remove + wallet view-library sync + preset/model hydration) in the same owner. Split the history-assumption setters and the inline view-row actions into their own action namespaces during dedicated optimizer action cleanup. The engine-backed proxy behavior added proxy-asset toggle, relationship-strength, apply-acknowledge, and reset actions beside the existing setters. Reference-only proxy support (catalog resolution, reference-instrument reconcile, prefetch, per-card proxy search action) extended it again. The per-wallet assumption library (per-entry sync from the save funnels, gap-fill hydrate action, card collapse overrides) extended it again. The Risk-weighted sizing cross-link added the argument-taking switch-objective-and-run action beside the menu apply action it mirrors; same cleanup. The History-assumptions queue restructure added the active-asset pointer the rail pills, Prev and Skip write, and retired the per-card collapse override it made write-only; same cleanup." :max-lines 951 :retire-by "2026-12-31"}
 {:path "src/hyperopen/portfolio/optimizer/application/history_loader/api_v2/alignment.cljs" :owner "portfolio" :reason "The failed-proxy forgiveness policy (a backend catalog-proxy lookback extension that fails validation must not reject an asset whose served series is its own usable native history - live 2026-07-07 STRK over-blocking) added the proxy-extension warning codes, own-lineage checks, and per-local-id series lookup to the hard-warning exclusion; extract a warning-policy helper namespace during dedicated optimizer history-loader cleanup. The calendar-poisoning recovery (live 2026-07-08: one 6-day listing disjoint from a stale-ended series emptied the shared calendar and excluded a whole 79-asset universe) added the served-observations projection and the common-gap peel rebindings; the peel algorithm itself lives in history-loader.calendar, and the rebinding block is the next extraction candidate in that same cleanup." :max-lines 580 :retire-by "2026-12-31"}
 {:path "src/hyperopen/portfolio/optimizer/application/request_builder.cljs" :owner "portfolio" :reason "Conservative history-assumption wiring (normalize, re-admit dropped no-history assets to the engine universe, mirror caps into per-asset-overrides) extended the request assembler; the fee-mode->bps resolver that derives :default-fee-bps from the canonical schedule (so the rebalance-preview fee component is no longer always $0) extended it again; extract request helpers during dedicated optimizer request-builder cleanup. The engine-backed proxy behavior added structural proxy classification, alignment exclusion of complete-assumption assets, proxy validation against usable history, and the short-overlap regression-series sub-alignment. Reference-only proxy alignment (align out-of-universe proxies for covariance, exclude them from the allocatable universe) extended it again. The universe-membership guard on the reference-drop (a stored reference that joined the universe stays allocatable) extended it again." :max-lines 685 :retire-by "2026-12-31"}
 {:path "src/hyperopen/portfolio/optimizer/application/setup_readiness.cljs" :owner "portfolio" :reason "Honest history-assumption gating (proxy-not-applied, incomplete-field guidance, missing-history-assumptions reason and copy) plus native-mark execution-price seeding (resolve live marks from the market catalog by coin into [:execution-assumptions :prices-by-id] so proxy-priced HIP-3 perps cost/size against the native mark, not an equity-proxy daily close) extended the readiness builder; extract assumption-warning and native-mark helpers during dedicated optimizer readiness cleanup. Humanized portfolio-level prior-fallback warning copy (missing-market-cap-prior / missing-current-portfolio-prior) added in the 2026-07-04 right-rail pass. The engine-backed proxy behavior added proxy completeness gating (self-proxy, unusable proxies, cap-above-global) and the assumption-required short-history blocking rule with its copy." :max-lines 650 :retire-by "2026-12-31"}
 {:path "test/hyperopen/portfolio/optimizer/contracts_test.cljs" :owner "portfolio" :reason "History-assumption draft spec coverage (valid proxy/conservative shapes, malformed rejection, additive migration backfill) extended the central optimizer contract suite; the worker-boundary codec regression for :current-portfolio-weights-by-instrument (rebalance-preview phantom-leg fix) extended it again; the :prices-by-id input-signature-exclusion regression (native-mark execution price) extended it again; the proxy-history-assumption removal added a spec-rejection test and a proxy->conservative migration test; the doubled-dex instrument-id canonicalization migration (persisted drafts carrying perp:xyz:xyz:* universe/constraint/assumption ids) extended it again; split history-assumption and codec spec tests during dedicated optimizer contract test cleanup. The engine-backed multi-proxy shape acceptance + pass-through-migration coverage extended it again. Reference-only proxy draft-key spec/migration coverage extended it again. The absolute→percentage net-band migration coverage (convert/clamp/reset/idempotence) extended it again." :max-lines 730 :retire-by "2026-12-31"}
 {:path "test/hyperopen/portfolio/optimizer/actions_test.cljs" :owner "portfolio" :reason "Optimizer route/scenario action coverage (route load effects, scenario save/archive/duplicate, objective menu) lives in one suite; the assumption-library route-load effect expectations nudged it past the 500-line default. Split route-load fixtures during dedicated optimizer action test cleanup." :max-lines 530 :retire-by "2026-12-31"}
 {:path "test/hyperopen/portfolio/optimizer/application/view_model_history_assumption_cards_test.cljs" :owner "portfolio" :reason "The history-assumption card view-model suite (split 2026-07-05) crossed the 500-line default when the proxy-workflow usability pass added coverage for the day-count-ascending dropdown, the collapse default/override matrix, and the unconfigured-above-configured card sort. Split the addable/collapse/sort projections into their own suite during dedicated optimizer view-model test cleanup." :max-lines 570 :retire-by "2026-12-31"}
 {:path "test/hyperopen/portfolio/optimizer/draft_actions_test.cljs" :owner "portfolio" :reason "History-assumption setter coverage extended the optimizer draft action suite; the return-views consolidation added inline view-row materialization coverage (author/update/clear/adopt-on-confidence/step, library sync, relative-view preservation). Split history-assumption and view-row action tests during dedicated optimizer action test cleanup. The engine-backed proxy action coverage (toggle membership, relationship strength, apply/reset acknowledgment) extended it again. Reference-only catalog proxy action coverage extended it again. Assumption-library sync/hydrate and card-collapse coverage extended it again. The stale-reference self-heal pin (field edits reconcile reference-only proxies) extended it again. The queue active-asset pointer coverage extended it again; retiring the per-card collapse override removed its two suites." :max-lines 1022 :retire-by "2026-12-31"}
 {:path "test/hyperopen/portfolio/optimizer/application/request_builder_test.cljs" :owner "portfolio" :reason "Conservative history-assumption folding coverage (re-admission, cap min-merge, proxy implied-correlation) extended the optimizer request-builder suite; the fee-mode->bps derivation coverage (:default-fee-bps from the canonical schedule) extended it again; split request-builder suites during dedicated optimizer request-builder test cleanup. The engine-backed proxy coverage (engine-backing, alignment-window preservation, tighter-cap wins, assumption-backed proxies rejected) extended it again. Reference-only proxy alignment/exclusion coverage extended it again. The covariance-window no-truncation equivalence pin (window with the proxy asset == window without it, plus the :history-window :return-observations count the card displays) extended it again. The api-v2 poisoned-calendar recovery pin (superset response re-intersected client-side while the regression sub-alignment keeps the backend overlap) extended it again. The stale-reference eviction pin (universe members listed as references stay allocatable) extended it again." :max-lines 855 :retire-by "2026-12-31"}
 {:path "test/hyperopen/portfolio/optimizer/application/setup_readiness_test.cljs" :owner "portfolio" :reason "History-assumption readiness coverage (complete-conservative allowed, proxy-not-applied, incomplete fields, objective-conditional expected-return) plus native-mark execution-price injection regression (incl. same-coin spot/perp decoy); split history-assumption readiness tests during dedicated optimizer readiness test cleanup. The engine-backed proxy readiness coverage (per-field blocking copy, short-history assumption-required threshold, window preservation) extended it again." :max-lines 780 :retire-by "2026-12-31"}
 {:path "src/hyperopen/portfolio/optimizer/domain/constraints.cljs" :owner "portfolio" :reason "Gross-leverage preservation added a signed-linear gross FLOOR (gross-floor-signs/gross-floor-spec single-sign guard, gross-capacity) plus presolve checks (:gross-floor-above-gross-max, :gross-floor-exceeds-capacity) so minimum-variance holds the book's current gross instead of delevering to the net floor; extract the gross-floor encoding helpers during dedicated optimizer constraint cleanup. The percentage-of-gross net band (net-band-spec, net-band-warnings, widened feasibility checks) extended it again; same cleanup." :max-lines 615 :retire-by "2026-12-31"}
 {:path "src/hyperopen/portfolio/optimizer/application/current_portfolio.cljs" :owner "portfolio" :reason "Unified-account NAV correction (spot-wallet collateral denominator for portfolio-margin accounts, whose exposure spans every dex) and the seeded gross-leverage floor (:gross-min derived from the current gross ratio) extended the current-portfolio snapshot/seed owner; the holdings-by-default work added the cheap per-source availability signature (perp clearinghouse vs spot balances) the preseed arrival watcher keys on; extract the NAV-source and constraint-seed helpers during dedicated optimizer current-portfolio cleanup. The percentage-of-gross net band changed the derived seed (net target + :net-band-pct)." :max-lines 575 :retire-by "2026-12-31"}
 {:path "test/hyperopen/portfolio/optimizer/execution_actions_test.cljs" :owner "platform" :reason "The stale-recommendation execution-gate coverage (confirm refuses a stale plan with no execute/unlock emitted, arm refuses to leave :staged, a current solved run still submits) extended the existing execution-action suite that already covers staging mutators, order-type re-resolution at confirm, the locked/unlocking/not-ready agent-readiness gates, and the resume/revert/re-stage recovery actions. Split the agent-readiness/staleness gate tests from the recovery-action tests during dedicated optimizer action test cleanup. The sell-excluded-holdings exit-toggle coverage (restage to sell-to-zero, revert to hold, pre-run guards, 2026-07-15) extended it again; the persisted auto-exit preference coverage (entry seeding with spot/engine-dropped exemptions, persist+restage toggle) extended it again. The restage-on-fresher-books action coverage (rebuild keeps the exit set; never touches an armed, submitting, or post-run surface) extended it again." :max-lines 830 :retire-by "2026-09-30"}
 {:path "src/hyperopen/portfolio/optimizer/actions/universe.cljs" :owner "portfolio" :reason "The holdings-by-default work extended the universe action owner with universe-source recording (holdings omission accounting for spot-excluded / unusable-history assets, custom flip on hand-adds) and the clear-universe escape hatch beside the existing add/remove/side/from-current mutations; split the from-current seeding path into its own namespace during dedicated optimizer action cleanup." :max-lines 570 :retire-by "2026-12-31"}
 {:path "src/hyperopen/runtime/effect_adapters/portfolio_optimizer.cljs" :owner "platform" :reason "The per-wallet draft restore effect wrapper (IndexedDB load-draft + hydrate/preseed dispatch env) crossed the default threshold in the optimizer effect-adapter facade that already hosts history/run/scenario/tracking/constraint-profile wrappers; the return-view library load/sync wrappers (per-wallet remembered views, effect-side timestamps, read-only persist skip) extended it; the execution env's open-orders refresh capture (named-dex working-order freshness) extended it again; the assumption-library load/sync wrappers (per-wallet remembered history assumptions) extended it again. The return-views file workflow (JSON download + file-picker import wrappers) extended it again. Split the scenario/persistence wrappers out during dedicated effect-adapter cleanup.; the reset-draft (New scenario) wrapper extended it again; the history-bundle stale-while-revalidate cache's load-history-cache! wrapper (threaded into the restore effect env) extended it again Dispatching the execution restage once fresh book snapshots land (2026-07-27) extended it again." :max-lines 690 :retire-by "2026-12-31"}
 {:path "test/hyperopen/runtime/effect_adapters/portfolio_optimizer_execution_test.cljs" :owner "platform" :reason "The named-dex open-orders refresh regression (resting ledger fires the forced per-dex frontendOpenOrders refresh; a fully filled run does not) extended the execution effect suite that already covers submit/reject/realized-slippage/reprice/resting/persistence outcomes. Split the outcome-classification tests from the persistence-workflow tests during dedicated effect-adapter test cleanup." :max-lines 550 :retire-by "2026-12-31"}
 {:path "src/hyperopen/portfolio/optimizer/actions/execution.cljs" :owner "platform" :reason "The working-order amend action (2026-07-04) — cancel+replace one resting order behind the full agent-readiness gate stack with refusal messaging — extended the execution action owner that already hosts staging mutators, the confirm gate stack, and the resume/revert/re-stage recovery actions. Split the recovery/amend actions from the staging mutators during dedicated optimizer action cleanup. The isolated-margin auto top-up intent seam at confirm extended it again. The sell-excluded-holdings exit toggle (modal exit set + plan restage from the stored run request, 2026-07-15) extended it again; the persisted auto-exit preference (entry candidate seeding + settings-persisting toggle action) extended it again. The restage-plan action (2026-07-27), which re-costs the staged plan in place when live books arrive, extended it again." :max-lines 710 :retire-by "2026-12-31"}
 {:path "test/hyperopen/views/portfolio/optimize/execution_tab_test.cljs" :owner "platform" :reason "Execution-tab suite covers staged KPIs, the type-aware slip/cost recompute, breakdown honesty, arm/confirm gating, and phase bands; the TWAP sliced price-cost KPI regression and the depth-floor lower-bound markers extended it past the 500-line default. Split KPI-strip coverage from phase-band coverage during dedicated optimizer view test cleanup. Partial-phase (rejection + live resting) regression added 2026-07-13. The health-rail book-data disclosure regression (flat estimate vs live book + age) extended it again." :max-lines 595 :retire-by "2026-12-31"}
 {:path "src/hyperopen/views/portfolio/optimize/execution_order_table.cljs" :owner "platform" :reason "The working-order amend editor (2026-07-04) — per-row reprice/convert-to-market controls with a live facts line and single commit affordance — extended the dense order-table owner (row rendering, inline staging type editor, per-row cost breakdown, skipped section). The failed-row exchange-error surface (truncated message + hover title) extended it again. Split the two inline editors from the table frame during dedicated optimizer view cleanup. The type-aware cost equation (TWAP worked-clips figures + note line, depth-floor lower-bound markers, book-coverage disclosure) extended it again. The excluded/held skipped-row rendering (hold chip, no fabricated notional) extended it again. The sell-excluded-holdings affordance (per-row Close instead + bulk control in the skipped section, exit chip + revert-to-hold in the row editor, and the persisted auto-exit setting strip, 2026-07-15) extended it again. The cost-equation honesty pass (2026-07-27) — an unknown fee renders as an em dash and suppresses the all-in total instead of printing a confident $0.00, and an unsliced TWAP row says it is the same flat estimate Market pays — extended it again." :max-lines 785 :retire-by "2026-12-31"}
 {:path "src/hyperopen/portfolio/optimizer/application/view_model/universe.cljs" :owner "portfolio" :reason "The engine-backed proxy behavior added the assumption-workflow badge states (Needs proxy / Proxy behavior), the paint-worthy badge set, and the assumption-required-observations projection beside the existing universe row/candidate models; split the assumption badge projection during dedicated optimizer view-model cleanup. The aligned-history (api-v2) observation sourcing arities extended it again. Renaming the configured proxy badge to \"Modeled\" added the per-badge hover-tooltip map and its row-model wiring, extending it again. The history-bundle error contract (2026-07-09) added the :stale-critical incident chip status (label + selected-history-status branch) for the backend's >= 7-day serve-time staleness escalation, extending it again." :max-lines 568 :retire-by "2026-12-31"}
 {:path "src/hyperopen/views/portfolio/optimize/target_exposure_table.cljs" :owner "portfolio" :reason "The Equal Risk correlation view (2026-07-11) added row click-to-select for the contribution-breakdown panel (selection attrs + effective-selection highlight keyed on the app-state instrument) and the per-row P&L-correlation-to-portfolio line to the existing allocation table owner (group/leg rows, side controls, add-asset popover, delta bars); split the add-asset popover into its own namespace during dedicated optimizer view cleanup." :max-lines 556 :retire-by "2026-12-31"}
 {:path "src/hyperopen/portfolio/optimizer/contracts/specs.cljs" :owner "portfolio" :reason "The Equal Risk :risk-structure result section (2026-07-11) — decomposition maps + capped underlying correlation matrix — extended the central solved-result payload validator alongside the existing draft/request/result predicates; split result-section predicates during dedicated optimizer contract cleanup. The :net-band-pct constraint field extended it again. The :inverse-volatility result section (sizing rows + seed weights + max sizing deviation) extended it again. The Milestone 6 analytics-parity pass widened the contribution/structure section comments to the covariance-only kinds (comments only, no validation change)." :max-lines 535 :retire-by "2026-12-31"}
 {:path "src/hyperopen/portfolio/optimizer/application/engine/payload.cljs" :owner "portfolio" :reason "The Risk-weighted sizing objective (2026-07-16) widened the dust-removal exemption to the covariance-only kinds and merged the :inverse-volatility sizing section beside the existing equal-risk section merge in the central result-payload assembler; split the objective-specific section merges during dedicated optimizer engine cleanup. Charging the canonical maker fee in the worker-built rebalance preview (2026-07-27), so both preview build sites agree and a resting order never reads as $0 all-in, extended it again." :max-lines 530 :retire-by "2026-12-31"}
 {:path "src/hyperopen/portfolio/optimizer/domain/objectives.cljs" :owner "portfolio" :reason "The percentage-of-gross net band (2026-07-11) added the coupled net-band inequality rows (net-band-inequalities) and the equality/range gating beside the existing solver-plan builders; split the constraint-row builders during dedicated optimizer objectives cleanup." :max-lines 545 :retire-by "2026-12-31"}
 {:path "test/hyperopen/portfolio/optimizer/domain/objectives_test.cljs" :owner "portfolio" :reason "The percentage-of-gross net band coverage (coupled-row encoding, realized-gross scaling, zero-band equality equivalence, nonzero-target shift, 100% no-restriction, closed-form disqualification) extended the solver-plan test suite; split constraint-row tests during dedicated optimizer objectives test cleanup." :max-lines 570 :retire-by "2026-12-31"}
 {:path "src/hyperopen/views/portfolio/optimize/results_diagnostics_rail.cljs" :owner "portfolio" :reason "The percentage-of-gross net band (2026-07-11) added the Net/Gross summary card (safe ratio formatter, — at zero gross) to the result diagnostics panels; split the diagnostics panel sections during dedicated optimizer results-view cleanup." :max-lines 530 :retire-by "2026-12-31"}
 {:path "src/hyperopen/views/portfolio/optimize/return_views_panel.cljs" :owner "portfolio" :reason "The Risk-weighted sizing review-fix pass (2026-07-16) widened the return-free gate to the shared setup-summary set and gave the covariance-only note copy a per-objective label case; split the note/summary copy helpers during dedicated optimizer views cleanup." :max-lines 515 :retire-by "2026-12-31"}
 {:path "src/hyperopen/views/portfolio/optimize/risk_contributions_card.cljs" :owner "portfolio" :reason "Two same-day features merged into the Risk Contribution Balance card owner (2026-07-16): the Equal Risk comprehension pass (shift display mode, Current/Shift columns, imbalance KPIs) and the Risk-weighted sizing floored-state cross-link (footer suggestion + switch-and-rerun button); each branch was under the cap alone. Split the shift-mode chart from the card frame during dedicated optimizer results-view cleanup." :max-lines 525 :retire-by "2026-12-31"}
]
